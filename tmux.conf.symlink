# Remap prefix to ctrl-s - keeps ctrl-a free for start of line
set -g prefix C-s
unbind C-b

# Who wants to ever send a C-s?!
# bind C-s send-prefix
bind C-s last-window

bind C-d detach
bind C-c new-window
bind r source-file ~/.tmux.conf
